//! Implementation of KvStore interface with persistent storage.
//!
//! The key/value data should be stored in the filesystem.
//! The key/value serialization method is up to the implementor.

#[cfg(test)]
mod tests {
    #[test]
    fn get_stored_value() {
        todo!();
    }

    #[test]
    fn overwrite_value() {
        todo!();
    }

    #[test]
    fn get_non_existent_value() {
        todo!();
    }

    #[test]
    fn remove_key() {
        todo!();
    }
}